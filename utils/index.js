const fcfsAnswer = require('./FCFS/fcfs-answer')
const sjfAnswer = require('./SJF/sjf-answer')
const priorityAnswer = require('./Priority/priority-answer')
const lrtfAnswer = require('./LRTF/lrtf-answer')
const srtfAnswer = require('./SRTF/srtf-answer')
const rrAnswer = require('./RR/rr-answer')

module.exports = {
    fcfsAnswer,
    sjfAnswer,
    priorityAnswer,
    lrtfAnswer,
    srtfAnswer,
    rrAnswer
}